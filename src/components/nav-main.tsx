'use client';

import { SidebarGroup, SidebarGroupContent } from '@/components/ui/sidebar';

export function NavMain() {
  return (
    <SidebarGroup>
      <SidebarGroupContent className="flex flex-col gap-2">
        {/* Main navigation items removed */}
      </SidebarGroupContent>
    </SidebarGroup>
  );
}
